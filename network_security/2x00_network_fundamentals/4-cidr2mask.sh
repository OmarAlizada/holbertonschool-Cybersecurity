#!/bin/bash
vals=(255 255 255 255 0); for i in 1 2 3 4; do [ $1 -ge $((i*8)) ] && o=255 || { [ $1 -le $(((i-1)*8)) ] && o=0 || { rem=$(($1%8)); o=$((256 - (1 << (8-rem)))); }; }; out+="$o."; done; printf "%s\n" "${out%.}"
